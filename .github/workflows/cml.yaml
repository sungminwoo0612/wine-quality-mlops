name: MLOps-CML-Report
on: [push]
permissions: write-all

jobs:
  run:
    runs-on: ubuntu-latest
    container: docker://ghcr.io/iterative/cml:0-dvc2-base1
    steps:
      - uses: actions/checkout@v3
      
      - name: Train model
        env:
          WANDB_API_KEY: ${{ secrets.WANDB_API_KEY }}
        run: |
          pip install -r requirements.txt
          dvc repro

      - name: Create CML Report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 1. 마크다운 리포트 작성
          echo "## 📊 Model Performance Report" > report.md
          
          # 2. 메트릭 추가
          echo "### Metrics" >> report.md
          cat metrics.json >> report.md
          
          # 3. Confusion Matrix 이미지 추가 (최신 문법 적용)
          echo "### Confusion Matrix" >> report.md
          # 구버전: cml-publish ...
          # 신버전: 아래처럼 마크다운 문법으로 이미지를 넣으면 CML이 알아서 업로드합니다.
          echo "![Confusion Matrix](./confusion_matrix.png)" >> report.md
          
          # 4. 리포트 전송 (최신 문법 적용)
          # 구버전: cml-send-comment report.md
          # 신버전: cml comment create report.md
          cml comment create report.md

          # 5. W&B 실험 결과
          echo "[W&B 실험 결과 보러가기](https://wandb.ai/sungminwoo/wine-quality-mlops)" >> report.md